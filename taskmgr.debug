#!/bin/bash

if [ $# -eq 0 ]; then
    CONFIG="./configs/rpi/tasks.json"
else
    CONFIG="$1"
fi
    rm ./maps/*
    source .venv/bin/activate
    PYTHONPATH=src \
        python3 -u -m pmtaskmgr.pmtaskmgr \
        --config "$CONFIG"
